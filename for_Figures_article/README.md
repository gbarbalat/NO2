proc_ens.pred.R  
- calculates weights of each basis learner to the ensemble 1 km predictions, weights are concatenated with concat_weights.R
- plots final predictions from each final model,including ensemble: mean over one year, for just one day, over Paris
- finds locations of 2022 NO2 predictions that are over the WHO threshold

calculate_metrics.R just calculate model performance for each year
